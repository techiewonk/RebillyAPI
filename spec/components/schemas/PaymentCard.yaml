type: object
required:
  - customerId
properties:
  id:
    description: The card identifier string
    readOnly: true
    allOf:
      - $ref: "#/components/schemas/ResourceId"
  customerId:
    description: The Customer's ID.
    allOf:
      - $ref: "#/components/schemas/ResourceId"
  method:
    description: The method of payment instrument.
    type: string
    readOnly: true
    enum:
      - payment-card
  pan:
    description: The card PAN (Primary Account Number). Required if card is not being created from Token. This is write-only.
    type: string
  bin:
    description: The card's bin (the PAN's first 6 digits)
    type: string
    format: bin
    readOnly: true
  last4:
    description: The PAN's last 4 digits
    type: string
    readOnly: true
  expYear:
    description: Card's expiry year. Required if card is not being created from Token.
    type: integer
  expMonth:
    description: Card's expiry month. Required if card is not being created from Token.
    type: integer
  cvv:
    description: Card's cvv (card verification value). Required if card is not being created from Token.
    type: string
  billingAddress:
    description: The Billing Address. Required if card is not being created from Token.
    allOf:
      - $ref: "#/components/schemas/ContactObject"
  token:
    description: PaymentCardToken. Use without any other fields except for `customerId`.
    type: string
  panFingerprint:
    description: A unique value to identify the PAN regardless of expiration dates.  It contains alphanumeric values.
    type: string
    readOnly: true
  status:
    description: |
      Payment Card status.  When a card is `active` it means it has been used at least once for an approved transaction.  
      To remove a card from being in use, set it as `deactivated` (see the deactivation endpoint).
    type: string
    readOnly: true
    enum:
      - active
      - expired
      - inactive
      - deactivated
      - pending
      - verification-needed
  brand:
    description: Payment Card brand
    type: string
    readOnly: true
    enum:
      - Visa
      - MasterCard
      - American Express
      - Discover
      - Maestro
      - Solo
      - Electron
      - JCB
      - Voyager
      - Diners Club
      - Switch
      - Laser
      - China UnionPay
      - AstroPay Card
  bankCountry:
    description: Payment Card bank country
    type: string
    readOnly: true
  bankName:
    description: Payment Card bank name
    type: string
    readOnly: true
  stickyGatewayAccountId:
    description: Default Gateway Account ID used for transactions
    type: string
    readOnly: true
  createdTime:
    description: Card created time
    allOf:
      - $ref: "#/components/schemas/ServerTimestamp"
  updatedTime:
    description: Card updated time
    allOf:
      - $ref: "#/components/schemas/ServerTimestamp"
  customFields:
    $ref: "#/components/schemas/ResourceCustomFields"
  _links:
    type: array
    description: The links related to resource
    readOnly: true
    minItems: 1
    items:
      anyOf:
      - $ref: "#/components/schemas/SelfLink"
      - $ref: "#/components/schemas/CustomerLink"
      - $ref: "#/components/schemas/AuthTransactionLink"
      - $ref: "#/components/schemas/ApprovalUrlLink"
